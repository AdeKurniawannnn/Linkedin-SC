{
    "scripts": {
        "setup": "$CONDUCTOR_ROOT_PATH/setup-workspace.sh && make install",
        "run": "make dev",
        "archive": "make clean; rm -rf LinkedinSC/frontend/.next LinkedinSC/backend/__pycache__ 2>/dev/null || true",
        "sync-env": "$CONDUCTOR_ROOT_PATH/sync-env.sh"
    },
    "runScriptMode": "nonconcurrent"
}
